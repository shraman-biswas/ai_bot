#ifndef MAIN_H
#define MAIN_H

#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define LEN 100

static char *str_upper(char *str);
static void disp_usage(const char *const str);

#endif